// import { ref } from 'vue'

// const filter = ref({})
// export { filter }

// const ret = { 123: 777 }

// const ret = ref({})

// export const useCatnowAdd = ( sell = false, buy = false, renta = false, need_renta = false ) => {
// ret.value = {'sell' : sell, 'buy':buy, 'renta':renta, 'need_renta':need_renta}
// return true
// }


// export const useCatnow = () => useState<object>('cat_now', () => ret.value )


// import { useRoute } from 'vue-router'

// const route = useRoute()
// const cat_id = route.params.id

// const config = useRuntimeConfig()

// const { data: catNow } = await useFetch(
//     // () => `${config.public.apiBase}/api/cat/` + cat_id,
//     () => `${config.public.apiBase}/api/cat/` + route.params.id
//   // //   //,
//   // //   //, { params: { token: 123 } }
//   )

// //   // // // watchEffect(()=>{
// //   useState('cat_now', () => catNow)

// export const useCatnow = () => useState<object>('cat_now', () => catNow)
